{
  "name": "@verifly/bank",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite preview",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@verifly/types": "workspace:*",
    "@verifly/ui": "workspace:*",
    "@verifly/utils": "workspace:*"
  },
  "devDependencies": {
    "@verifly/config": "workspace:*"
  }
}
