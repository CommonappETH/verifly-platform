{
  "name": "@verifly/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:dev": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env prod",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint config added in Phase 4)\"",
    "test": "echo \"(tests added in Phase 13)\""
  },
  "dependencies": {
    "@noble/hashes": "^1.5.0",
    "drizzle-orm": "^0.36.4",
    "hono": "^4.6.12",
    "nanoid": "^5.0.9",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20241218.0",
    "@types/bun": "^1.1.14",
    "@verifly/config": "workspace:*",
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.95.0"
  }
}
